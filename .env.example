# NASA ADS API token — get one at https://ui.adsabs.harvard.edu/user/settings/token
ADS_API_TOKEN=

# Where the local paper library lives. Defaults to the repo root if unset.
# LIT_CACHE_DIR=/Users/you/research/library
